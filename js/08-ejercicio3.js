/* 🔢 Contador de likes
Contexto:

En una red social, cada vez que un usuario hace clic en un botón de “Me gusta”, el sistema debe aumentar el
contador de likes y mostrar el total actualizado.

Consigna:

Crear una función que incremente un contador de likes y muestre el resultado en la consola.

Tiempo: 20 minutos

Paso a paso:

Definir una variable global llamada likes inicializada en 0.

Crear una función llamada incrementarLikes().

Dentro de la función, aumentar el valor de likes en 1.

Mostrar el número total de likes usando console.log().

Llamar a la función varias veces para comprobar su funcionamiento. */
